/**
you are given an array of integers nums and an integer k.

Return the total number of continuous subarrays whose sum equals k.

Input: nums = [1, 1, 1], k = 2
Output: 2 
// Explanation: The two subarrays are [1, 1] at index 0 and [1, 1] at index 1.

Input: nums = [-1, 1, 0, -2. 2, 0, 3, -3] k = -2
Output: 2
// Explanation: The two subarrays are [1, 2] and [3].
 */

function subArraySubEquals(nums: number[], k: number): number {
    return k
} 